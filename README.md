after retreiving the from the git repository
1. On CMD type "npm install" to install the package.json
2. On CMD type "npm run start" or "npm run dev" to start the environment
      * the CMD must output "SERVER RUNNING ON PORT 6000"
3. open browser or Postman and enter "localhost:6000/api"
